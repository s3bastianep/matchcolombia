# Google Maps (opcional): agrega API key para tiles de Google.
# Sin key se usa OpenStreetMap — mapa 100% interactivo (arrastrar, zoom, pines).

VITE_GOOGLE_MAPS_API_KEY=
# URL canónica del sitio (SEO, Open Graph, sitemap, JSON-LD)
# Ejemplo producción: https://matchcolombia.co o https://www.lumorahome.co
VITE_SITE_URL=https://matchcolombia.co

# Supabase — backend real (ver BACKEND.md)
# Sin estas variables la app usa localStorage (modo demo)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Solo para scripts locales (npm run seed:supabase) — NO subir a Railway
SUPABASE_SERVICE_ROLE_KEY=

# Railway — backend + PostgreSQL en un solo proyecto (ver RAILWAY.md)
VITE_USE_RAILWAY_API=false
# VITE_API_URL=   # vacío = /api en el mismo dominio

# Servidor (local o Railway)
DATABASE_URL=
JWT_SECRET=
UPLOAD_DIR=
PGSSL=false
